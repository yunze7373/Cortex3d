# Cortex3d 完整参数使用指南

## 🎯 概述

这是 Cortex3d `generate_character.py` 的完整使用指南，包含各种实际场景的参数组合示例，从基础到高级，从简单到复杂的完整覆盖。

## 📚 目录

1. [🚀 快速开始](#快速开始)
2. [📝 基础生成场景](#基础生成场景)
3. [🖼️ 图像输入场景](#图像输入场景)
4. [🎨 风格控制场景](#风格控制场景)
5. [👁️ 视角配置场景](#视角配置场景)
6. [📐 质量控制场景](#质量控制场景)
7. [🚀 3D转换场景](#3d转换场景)
8. [👗 换装编辑场景](#换装编辑场景)
9. [🛠️ 专业编辑场景](#专业编辑场景)
10. [🔧 处理优化场景](#处理优化场景)
11. [🔍 智能验证场景](#智能验证场景)
12. [⚙️ 高级配置场景](#高级配置场景)
13. [🔄 批处理场景](#批处理场景)
14. [🐛 故障排除场景](#故障排除场景)

---

## 🚀 快速开始

### 最简单的使用
```bash
# 纯文本描述生成
python scripts/generate_character.py "可爱的机器人"

# 从图片生成
python scripts/generate_character.py --input photo.jpg

# 生成并预览
python scripts/generate_character.py "魔法师" --preview
```

### 常用组合
```bash
# 动漫风格 + 多视角 + 预览
python scripts/generate_character.py "少女战士" --anime -v 8 --preview

# 高质量 + 3D转换
python scripts/generate_character.py "龙骑士" --res 4K --pro --to-3d

# 赛博朋克风格 + 快速模式
python scripts/generate_character.py "黑客" --cyberpunk --res 1K --mode local
```

---

## 📝 基础生成场景

### 纯文本描述生成
```bash
# 简单角色
python scripts/generate_character.py "可爱的猫耳少女"

# 详细描述
python scripts/generate_character.py "身穿银色盔甲的精灵战士，手持发光长剑，背景是魔法森林"

# 多元素描述
python scripts/generate_character.py "赛博朋克风格的女黑客，紫色头发，LED眼镜，霓虹灯背景"

# 特定职业
python scripts/generate_character.py "中世纪铁匠，肌肉发达，手持铁锤，火花四溅的铁砧前"
```

### 不同模式生成
```bash
# AiProxy云服务模式 (推荐)
python scripts/generate_character.py "武士" --mode proxy --token YOUR_TOKEN

# 直连Gemini API模式  
python scripts/generate_character.py "魔法师" --mode direct --token YOUR_API_KEY

# 本地Z-Image模式
python scripts/generate_character.py "机器人" --mode local --local-url http://localhost:8199
```

---

## 🖼️ 图像输入场景

### 基础图像参考
```bash
# 从图片提取角色特征
python scripts/generate_character.py --input reference_images/model.jpg

# 指定输出目录
python scripts/generate_character.py --input photo.jpg --output my_outputs

# 严格复制模式
python scripts/generate_character.py --input portrait.jpg --strict
```

### 图像预处理
```bash
# 自动去背景
python scripts/generate_character.py --input messy_photo.jpg --preprocess

# 指定去背景模型
python scripts/generate_character.py --input photo.jpg --preprocess --preprocess-model u2net

# 组合预处理和风格
python scripts/generate_character.py --input photo.jpg --preprocess --anime
```

### 图像分析控制
```bash
# 自动分析API选择
python scripts/generate_character.py --input photo.jpg --analysis-api auto

# 强制使用特定分析API
python scripts/generate_character.py --input photo.jpg --analysis-api proxy

# 跳过图像分析，直接生成
python scripts/generate_character.py --input photo.jpg --analysis-api local --strict
```

### 图像+描述组合
```bash
# 图像参考 + 文字描述修改
python scripts/generate_character.py "添加火焰翅膀" --input angel.jpg

# 图像参考 + 风格转换
python scripts/generate_character.py --input realistic_photo.jpg --anime

# 图像参考 + 详细指令
python scripts/generate_character.py "换成蓝色头发，添加机械臂" --input character.jpg
```

---

## 🎨 风格控制场景

### 基础风格预设
```bash
# 写实摄影风格
python scripts/generate_character.py "模特" --photorealistic --res 4K

# 日式动漫风格
python scripts/generate_character.py "学生" --anime

# 美式漫画风格
python scripts/generate_character.py "超级英雄" --comic

# 3D卡通风格
python scripts/generate_character.py "玩具角色" --3d-toon
```

### 艺术风格预设
```bash
# 水彩画风格
python scripts/generate_character.py "仙女" --watercolor

# 油画风格
python scripts/generate_character.py "贵族肖像" --oil

# 吉卜力风格
python scripts/generate_character.py "森林精灵" --ghibli

# 极简风格
python scripts/generate_character.py "现代设计师" --minimal
```

### 游戏/数字风格
```bash
# 像素艺术
python scripts/generate_character.py "勇者" --pixel

# Minecraft风格
python scripts/generate_character.py "史蒂夫" --minecraft

# 低多边形风格
python scripts/generate_character.py "未来战士" --lowpoly
```

### 材质风格
```bash
# 粘土动画风格
python scripts/generate_character.py "可爱动物" --clay

# 毛绒玩具风格
python scripts/generate_character.py "泰迪熊" --plush

# 纸质工艺风格
python scripts/generate_character.py "纸片人" --paper
```

### 主题风格
```bash
# 赛博朋克风格
python scripts/generate_character.py "网络黑客" --cyberpunk

# 奇幻中世纪风格
python scripts/generate_character.py "骑士" --fantasy
```

### 自定义风格
```bash
# 自定义风格描述
python scripts/generate_character.py "艺术家" --style "impressionist painting with vibrant colors"

# 风格组合描述
python scripts/generate_character.py "机器人" --style "steampunk Victorian era mechanical design"

# 详细风格指令
python scripts/generate_character.py "法师" --style "dark fantasy gothic art, dramatic lighting, detailed textures"
```

### 查看可用风格
```bash
# 列出所有风格预设
python scripts/generate_character.py --list-styles
```

---

## 👁️ 视角配置场景

### 标准视角设置
```bash
# 4视角 (默认: 前后左右)
python scripts/generate_character.py "战士" --views 4

# 6视角 (增加45度角)
python scripts/generate_character.py "舞者" -v 6

# 8视角 (增加顶部底部)
python scripts/generate_character.py "飞行员" -v 8
```

### 自定义视角
```bash
# 指定特定视角
python scripts/generate_character.py "模特" --custom-views front right back

# 仅正面和侧面
python scripts/generate_character.py "肖像" --custom-views front left right

# 包含特殊视角
python scripts/generate_character.py "超人" --custom-views front back top bottom

# 完整8视角自定义
python scripts/generate_character.py "角色" --custom-views front front_right right back back_left left top bottom
```

### 迭代360度模式 (高一致性)
```bash
# 4视角迭代模式 (需要参考图)
python scripts/generate_character.py --input character.jpg --iterative-360 4

# 6视角迭代模式
python scripts/generate_character.py --input model.jpg --iterative-360 6 --strict

# 8视角迭代模式 + 高质量
python scripts/generate_character.py --input reference.jpg --iterative-360 8 --pro --res 4K
```

### 视角 + 风格组合
```bash
# 动漫风格多视角
python scripts/generate_character.py "魔法少女" --anime -v 8

# 像素风格4视角
python scripts/generate_character.py "勇者" --pixel --views 4

# 赛博朋克风格自定义视角
python scripts/generate_character.py "黑客" --cyberpunk --custom-views front right back
```

---

## 📐 质量控制场景

### 分辨率控制
```bash
# 快速模式 (1K)
python scripts/generate_character.py "测试角色" --res 1K

# 标准模式 (2K，默认)
python scripts/generate_character.py "正常角色" --res 2K

# 高质量模式 (4K)
python scripts/generate_character.py "精细角色" --res 4K
```

### 模型选择
```bash
# 使用默认推荐模型
python scripts/generate_character.py "角色" --model auto

# 使用Pro高质量模型
python scripts/generate_character.py "重要角色" --pro

# 指定特定模型
python scripts/generate_character.py "角色" --model "gemini-3-pro-image-preview"
```

### 宽高比控制
```bash
# 正方形 (1:1)
python scripts/generate_character.py "头像" --ratio 1:1

# 标准横屏 (3:2)
python scripts/generate_character.py "角色" --ratio 3:2

# 竖屏模式 (2:3)
python scripts/generate_character.py "全身像" --ratio 2:3

# 宽屏模式 (16:9)
python scripts/generate_character.py "场景角色" --ratio 16:9
```

### 质量组合优化
```bash
# 最高质量设置
python scripts/generate_character.py "masterpiece" --pro --res 4K --ratio 1:1

# 平衡质量速度
python scripts/generate_character.py "日常角色" --res 2K

# 快速预览模式
python scripts/generate_character.py "草图" --res 1K --no-cut
```

---

## 🚀 3D转换场景

### 基础3D转换
```bash
# 自动2D转3D
python scripts/generate_character.py "机器人" --to-3d

# 简写形式
python scripts/generate_character.py "角色" --3d
```

### 3D算法选择
```bash
# HunYuan3D 标准版 (默认，速度快)
python scripts/generate_character.py "战士" --3d --algo hunyuan3d

# HunYuan3D 改进版 (质量更好)
python scripts/generate_character.py "法师" --3d --algo hunyuan3d-2.1

# HunYuan3D 全能版 (支持姿势控制)
python scripts/generate_character.py "舞者" --3d --algo hunyuan3d-omni

# Trellis 高质量重建
python scripts/generate_character.py "精细模型" --3d --algo trellis

# Trellis2 最新版本
python scripts/generate_character.py "高端角色" --3d --algo trellis2
```

### 3D质量控制
```bash
# 快速模式
python scripts/generate_character.py "测试" --3d --3d-quality balanced

# 标准模式 (默认)
python scripts/generate_character.py "角色" --3d --3d-quality high

# 最高质量模式
python scripts/generate_character.py "重要角色" --3d --3d-quality ultra
```

### 几何体模式 (快速)
```bash
# 仅生成几何体，无纹理
python scripts/generate_character.py "快速模型" --3d --geometry-only

# 简写形式
python scripts/generate_character.py "草模" --3d --fast-3d

# 快速几何体 + 预览
python scripts/generate_character.py "测试模型" --3d --fast-3d --preview
```

### 姿势控制 (HunYuan3D-Omni)
```bash
# 使用T-Pose
python scripts/generate_character.py "角色" --3d --algo hunyuan3d-omni --pose poses/t_pose.json

# 使用A-Pose
python scripts/generate_character.py "角色" --3d --algo hunyuan3d-omni --pose poses/a_pose.json

# 自定义姿势
python scripts/generate_character.py "舞者" --3d --algo hunyuan3d-omni --pose my_poses/dance.json
```

### 完整3D流水线
```bash
# 高质量完整流水线
python scripts/generate_character.py "英雄" --input hero.jpg --anime -v 8 --3d --algo hunyuan3d-2.1 --3d-quality ultra --preview

# 快速原型流水线
python scripts/generate_character.py "测试角色" --pixel --3d --fast-3d --res 1K

# 专业级流水线
python scripts/generate_character.py --input reference.jpg --strict --pro --res 4K -v 8 --3d --algo trellis2 --3d-quality ultra
```

---

## 👗 换装编辑场景

### 基础换装
```bash
# 简单换装
python scripts/generate_character.py --input model.jpg --wear dress.png

# 多件服装
python scripts/generate_character.py --input model.jpg --wear top.png bottom.png

# 换装+描述
python scripts/generate_character.py --input model.jpg --wear dress.png "穿上优雅的晚礼服"
```

### 配饰添加
```bash
# 单个配饰
python scripts/generate_character.py --input character.jpg --accessory hat.png

# 多个配饰
python scripts/generate_character.py --input model.jpg --accessory hat.png bag.png shoes.png

# 配饰+换装组合
python scripts/generate_character.py --input model.jpg --wear dress.png --accessory necklace.png
```

### 换装模式控制
```bash
# 严格保留原角色特征 (默认)
python scripts/generate_character.py --input model.jpg --wear dress.png --wear-strict

# 快速换装模式
python scripts/generate_character.py --input model.jpg --wear outfit.png --wear-model flash

# 高保真换装模式
python scripts/generate_character.py --input model.jpg --wear dress.png --wear-model pro
```

### 自定义换装指令
```bash
# 自定义换装描述
python scripts/generate_character.py --input model.jpg --wear dress.png --wear-instruction "让她穿上这件红色晚礼服，保持优雅姿态"

# 跳过服装预处理
python scripts/generate_character.py --input model.jpg --wear messy_dress.png --wear-no-rembg
```

### 换装+多视角
```bash
# 换装后生成多视角
python scripts/generate_character.py --input model.jpg --wear dress.png -v 8

# 换装+风格+多视角
python scripts/generate_character.py --input model.jpg --wear dress.png --anime -v 6

# 换装+3D转换
python scripts/generate_character.py --input model.jpg --wear outfit.png --3d
```

---

## 🛠️ 专业编辑场景

### 元素编辑模式
```bash
# 添加元素
python scripts/generate_character.py --mode-edit --edit-elements "add:火焰翅膀" --from-edited character.jpg

# 移除元素
python scripts/generate_character.py --mode-edit --edit-elements "remove:帽子" --from-edited model.jpg

# 修改元素
python scripts/generate_character.py --mode-edit --edit-elements "modify:头发改成蓝色" --from-edited portrait.jpg

# 多个编辑操作
python scripts/generate_character.py --mode-edit --edit-elements "add:机械臂,remove:背景" --from-edited character.jpg
```

### 细节修复模式
```bash
# 修复面部问题
python scripts/generate_character.py --mode-refine --refine-details face --detail-issue "眼睛不对称" --from-refine portrait.jpg

# 修复手部问题
python scripts/generate_character.py --mode-refine --refine-details hands --detail-issue "左手有6根手指" --from-refine character.jpg

# 修复姿势问题
python scripts/generate_character.py --mode-refine --refine-details pose --detail-issue "身体比例不协调" --from-refine fullbody.jpg

# 修复眼部细节
python scripts/generate_character.py --mode-refine --refine-details eyes --detail-issue "眼睛颜色不一致" --from-refine face.jpg

# 自定义问题修复
python scripts/generate_character.py --mode-refine --refine-details custom --detail-issue "武器细节模糊" --from-refine warrior.jpg
```

### 风格转换模式
```bash
# 使用风格预设转换
python scripts/generate_character.py --mode-style --style-preset anime --from-style photo.jpg

# 电影级风格转换
python scripts/generate_character.py --mode-style --style-preset cinematic --from-style portrait.jpg

# 油画风格转换
python scripts/generate_character.py --mode-style --style-preset oil-painting --from-style character.jpg

# 自定义风格转换
python scripts/generate_character.py --mode-style --custom-style "impressionist watercolor with soft brushstrokes" --from-style model.jpg

# 保留细节的风格转换
python scripts/generate_character.py --mode-style --style-preset comic --preserve-details --from-style realistic.jpg
```

### 图像合成模式
```bash
# 基础合成
python scripts/generate_character.py --mode-composite --composite-images model.png dress.png --composite-instruction "让模特穿上这件裙子"

# 多图合成
python scripts/generate_character.py --mode-composite --composite-images person.jpg hat.png bag.png shoes.png --composite-instruction "为角色添加完整搭配"

# 指定合成类型
python scripts/generate_character.py --mode-composite --composite-images model.jpg outfit.png --composite-type clothing --composite-instruction "换装"

# 配饰类型合成
python scripts/generate_character.py --mode-composite --composite-images character.jpg accessories.png --composite-type accessory

# 通用合成
python scripts/generate_character.py --mode-composite --composite-images bg.jpg character.png --composite-type general --composite-instruction "将角色放在背景中"
```

### 高保真编辑模式
```bash
# 基础高保真编辑
python scripts/generate_character.py --mode-preserve --preserve-image model.jpg --preserve-instruction "添加公司logo到T恤上"

# 保留特定细节
python scripts/generate_character.py --mode-preserve --preserve-image portrait.jpg --preserve-detail-desc "完全保持面部特征不变" --preserve-instruction "更换发型"

# 添加元素的高保真编辑
python scripts/generate_character.py --mode-preserve --preserve-image character.jpg --preserve-element logo.png --preserve-instruction "将logo添加到胸前"

# 复杂高保真编辑
python scripts/generate_character.py --mode-preserve --preserve-image model.jpg --preserve-element accessory.png --preserve-detail-desc "保持人物表情和姿势" --preserve-instruction "添加配饰但不改变整体风格"
```

### 编辑输出控制
```bash
# 自定义编辑输出文件名
python scripts/generate_character.py --mode-edit --edit-elements "add:翅膀" --from-edited angel.jpg --edit-output-name "angel_with_wings"

# 多种编辑组合
python scripts/generate_character.py --mode-refine --refine-details face --detail-issue "优化面部细节" --from-refine portrait.jpg --edit-output-name "refined_portrait"
```

---

## 🔧 处理优化场景

### 切割控制
```bash
# 禁用自动切割
python scripts/generate_character.py "角色" --no-cut

# 简写形式
python scripts/generate_character.py "角色" --no-crop

# 切割+预览
python scripts/generate_character.py "角色" --preview  # 默认启用切割
```

### 主体隔离
```bash
# 仅处理主体，移除背景
python scripts/generate_character.py --input complex_scene.jpg --subject-only

# 简写形式
python scripts/generate_character.py --input photo.jpg --isolate

# 主体隔离+保留特定道具
python scripts/generate_character.py --input scene.jpg --subject-only --with-props guitar bicycle

# 主体隔离+风格转换
python scripts/generate_character.py --input photo.jpg --isolate --anime
```

### 负面提示词控制
```bash
# 禁用所有负面提示词
python scripts/generate_character.py "角色" --no-negative

# 指定负面提示词类别
python scripts/generate_character.py "角色" --negative-categories anatomy quality

# 仅解剖学负面提示词
python scripts/generate_character.py "角色" --negative-categories anatomy

# 完整负面提示词 (默认)
python scripts/generate_character.py "角色" --negative-categories anatomy quality layout
```

### 调试和测试
```bash
# 仅导出提示词，不调用API
python scripts/generate_character.py "角色" --export-prompt

# 简写形式
python scripts/generate_character.py "角色" --dry-run

# 导出提示词+参数预览
python scripts/generate_character.py "复杂角色" --anime -v 8 --res 4K --dry-run
```

### 预览控制
```bash
# 生成后自动预览
python scripts/generate_character.py "角色" --preview

# 生成+3D+预览
python scripts/generate_character.py "角色" --3d --preview

# 禁用预览 (默认)
python scripts/generate_character.py "角色"  # 无 --preview 参数
```

---

## 🔍 智能验证场景

### 自动视角验证与补全
```bash
# 自动检测并补全缺失视角
python scripts/generate_character.py "战士" -v 8 --auto-complete

# 简写形式
python scripts/generate_character.py "角色" -v 8 --smart-fix

# 补全+最大重试次数
python scripts/generate_character.py "角色" -v 8 --smart-fix --max-retries 5

# 补全+高质量
python scripts/generate_character.py "英雄" --input hero.jpg -v 8 --smart-fix --pro
```

### 仅验证模式
```bash
# 仅验证，不补全
python scripts/generate_character.py --input existing_result.png --validate-only

# 简写形式
python scripts/generate_character.py --input result.png --check-only

# 验证特定视角要求
python scripts/generate_character.py --input multi_view.png --check-only --custom-views front right back left
```

### 验证+补全组合
```bash
# 先验证，如果不完整则补全
python scripts/generate_character.py "角色" -v 8 --smart-fix

# 验证现有结果并补全
python scripts/generate_character.py --input partial_result.png -v 8 --smart-fix --input hero.jpg

# 智能补全+风格一致
python scripts/generate_character.py --input front_view.jpg -v 8 --smart-fix --anime --strict
```

---

## ⚙️ 高级配置场景

### 本地服务配置
```bash
# 指定本地服务地址
python scripts/generate_character.py "角色" --mode local --local-url http://192.168.1.100:8199

# 调整推理步数
python scripts/generate_character.py "角色" --mode local --local-steps 12

# 本地模式+高质量
python scripts/generate_character.py "角色" --mode local --local-steps 15 --res 4K
```

### API配置
```bash
# 强制使用代理分析
python scripts/generate_character.py --input photo.jpg --analysis-api proxy

# 强制直连Gemini分析
python scripts/generate_character.py --input photo.jpg --analysis-api direct

# 跳过图像分析
python scripts/generate_character.py --input photo.jpg --analysis-api local --strict
```

### 认证配置
```bash
# 手动指定Token (覆盖环境变量)
python scripts/generate_character.py "角色" --mode proxy --token "your-aiproxy-token"

# 直连模式手动指定API Key
python scripts/generate_character.py "角色" --mode direct --token "your-gemini-api-key"

# 使用别名
python scripts/generate_character.py "角色" --mode proxy --auth "your-token"
```

### 输出目录配置
```bash
# 自定义输出目录
python scripts/generate_character.py "角色" --output my_characters

# 简写形式
python scripts/generate_character.py "角色" -o results

# 按日期组织输出
python scripts/generate_character.py "角色" -o "outputs/$(date +%Y-%m-%d)"
```

### 复杂高级组合
```bash
# 完整高级配置
python scripts/generate_character.py "高级角色" \
  --mode direct \
  --token "your-api-key" \
  --input reference.jpg \
  --analysis-api direct \
  --pro \
  --res 4K \
  -v 8 \
  --iterative-360 8 \
  --anime \
  --3d \
  --algo trellis2 \
  --3d-quality ultra \
  --smart-fix \
  --max-retries 3 \
  --preview \
  -o premium_outputs
```

---

## 🔄 批处理场景

### 多角色批量生成
```bash
# 批量基础角色
for character in "战士" "法师" "弓箭手" "盗贼"; do
  python scripts/generate_character.py "$character" --fantasy --preview -o "rpg_characters"
done

# 批量风格变化
for style in "anime" "pixel" "clay" "watercolor"; do
  python scripts/generate_character.py "英雄" --$style -o "style_comparison"
done

# 批量质量测试
for res in "1K" "2K" "4K"; do
  python scripts/generate_character.py "测试角色" --res $res -o "quality_test"
done
```

### 批量图像处理
```bash
# 批量转换现有图片
for img in reference_images/*.jpg; do
  python scripts/generate_character.py --input "$img" --anime -v 8 -o "anime_conversion"
done

# 批量3D转换
for img in 2d_results/*.png; do
  python scripts/generate_character.py --from-id "$(basename $img .png)" --3d --fast-3d
done

# 批量风格转换
for img in portraits/*.jpg; do
  python scripts/generate_character.py --mode-style --style-preset anime --from-style "$img" -o "style_converted"
done
```

### 批量质量优化
```bash
# 批量高质量重新生成
for desc in "勇者" "公主" "巨龙" "魔王"; do
  python scripts/generate_character.py "$desc" --pro --res 4K --3d-quality ultra --3d -o "premium_collection"
done

# 批量智能补全
for result in partial_results/*.png; do
  python scripts/generate_character.py --input "$result" -v 8 --smart-fix
done
```

---

## 🐛 故障排除场景

### 调试生成问题
```bash
# 调试模式：仅导出提示词
python scripts/generate_character.py "问题角色" --dry-run

# 最简配置测试
python scripts/generate_character.py "简单测试" --res 1K --no-negative

# 本地模式测试
python scripts/generate_character.py "本地测试" --mode local
```

### 网络和API问题
```bash
# 切换到不同API
python scripts/generate_character.py "角色" --mode direct  # 如果proxy有问题

# 降低负载
python scripts/generate_character.py "角色" --res 1K --views 4

# 跳过复杂功能
python scripts/generate_character.py "角色" --no-negative --no-cut
```

### 质量问题诊断
```bash
# 最高质量设置诊断
python scripts/generate_character.py "测试" --pro --res 4K --iterative-360 4 --input reference.jpg

# 对比不同算法
python scripts/generate_character.py "对比测试" --3d --algo hunyuan3d -o "algo_test_1"
python scripts/generate_character.py "对比测试" --3d --algo trellis -o "algo_test_2"

# 验证视角完整性
python scripts/generate_character.py --input result.png --check-only
```

### 性能优化
```bash
# 快速模式
python scripts/generate_character.py "快速角色" --res 1K --views 4 --fast-3d

# 本地模式 (无网络依赖)
python scripts/generate_character.py "离线角色" --mode local

# 跳过非必要处理
python scripts/generate_character.py "高效角色" --no-cut --no-negative --dry-run
```

---

## 💡 使用技巧和最佳实践

### 🎯 效果最佳的组合
```bash
# 动漫角色最佳流水线
python scripts/generate_character.py --input photo.jpg --anime -v 8 --res 4K --3d --algo hunyuan3d-2.1 --smart-fix

# 写实角色最佳流水线
python scripts/generate_character.py "professional model" --photorealistic --pro --res 4K -v 6 --3d --algo trellis2 --3d-quality ultra

# 游戏角色最佳流水线
python scripts/generate_character.py "RPG hero" --fantasy --pixel -v 8 --3d --fast-3d --preview

# 概念艺术最佳流水线
python scripts/generate_character.py "concept art character" --watercolor --pro --res 4K --ratio 16:9 --preview
```

### ⚡ 速度优化组合
```bash
# 最快预览模式
python scripts/generate_character.py "quick test" --res 1K --views 4 --mode local --no-cut

# 快速3D原型
python scripts/generate_character.py "prototype" --pixel --res 1K --3d --fast-3d

# 快速批量处理
python scripts/generate_character.py "batch item" --res 1K --no-negative --no-cut --dry-run
```

### 🎨 创意实验组合
```bash
# 风格混合实验
python scripts/generate_character.py "artist" --style "cyberpunk watercolor with anime influences"

# 多视角风格对比
python scripts/generate_character.py "style test" --ghibli -v 8 --custom-views front right back

# 3D算法对比
python scripts/generate_character.py "algorithm test" --3d --algo hunyuan3d --3d-quality ultra -o "test_hunyuan"
python scripts/generate_character.py "algorithm test" --3d --algo trellis2 --3d-quality ultra -o "test_trellis"
```

---

## 📋 参数速查表

### 🔤 常用简写对照
```
-v        ← --views
-o        ← --output
--res     ← --resolution
--3d      ← --to-3d
--fast-3d ← --geometry-only
--dry-run ← --export-prompt
--smart-fix ← --auto-complete
--check-only ← --validate-only
--isolate ← --subject-only
--clean   ← --preprocess
```

### 🎨 风格速查
```
--photorealistic  # 写实摄影
--anime          # 日式动漫
--pixel          # 像素艺术
--cyberpunk      # 赛博朋克
--ghibli         # 宫崎骏风格
--watercolor     # 水彩画
--clay           # 粘土动画
--minecraft      # 方块风格
```

### 🚀 3D算法速查
```
hunyuan3d        # 标准版 (默认)
hunyuan3d-2.1    # 改进版
hunyuan3d-omni   # 支持姿势控制
trellis          # 高质量
trellis2         # 最新版本
```

### ✨ 质量等级速查
```
--res 1K         # 快速
--res 2K         # 标准 (默认)
--res 4K         # 高质量
--pro            # Pro模型
--3d-quality ultra # 3D最高质量
```

---

## 🔗 相关资源

- 📖 [完整参数文档](generate_character_参数文档.md)
- 🚀 [参数重构指南](参数重构指南_v2.md) 
- 🎨 [风格预设参考](../prompts/styles/)
- 🔧 [配置文件说明](../scripts/config.py)
- 🐛 [故障排除指南](故障排除指南.md)

---

*本指南涵盖了 Cortex3d 的所有主要使用场景。如需更多帮助，请运行 `python scripts/generate_character.py --help` 查看最新参数说明。*